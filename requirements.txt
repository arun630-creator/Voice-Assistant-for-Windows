# Nova Voice Assistant — Python dependencies
# Install with:  pip install -r requirements.txt

# Audio
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.10.0
faster-whisper==1.1.0
vosk==0.3.45

# Text-to-Speech
pyttsx3==2.98

# LLM (Ollama REST client)
requests>=2.31.0

# UI automation (for follow-up actions like auto-sending WhatsApp)
pyautogui>=0.9.54

# Volume control
pycaw>=20251023
comtypes>=1.4.0

# System tray / desktop app
pystray>=0.19.5
Pillow>=10.0.0

# Windows system integration
pywin32>=306

# Build tools (optional — only needed to create .exe)
# pyinstaller>=6.0
